[package]
name = "authn"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror.workspace = true
strum.workspace = true

uuid.workspace = true
chrono.workspace = true
async-trait.workspace = true
base64.workspace = true

tokio.workspace = true

sqlx.workspace = true

testcontainers-modules.workspace = true

# crypto
rand_core = "0.10.1"
rand = "0.10.2"
sha2 = "0.11.0"
subtle = "2.6.1"
argon2 = { version = "0.5.3", features = ["std"] } # std is added to resolve OsRng not found issue
